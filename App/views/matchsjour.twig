{% extends "template.twig" %}

{% block title %}Be-Pronostic{% endblock %}

{% block title_content %} Match du jour {% endblock %}

{% block content %}

    <form class="form-signin" action="{{ app.url_generator.generate('period') }}" method="POST">
        <label >Veuillez saisir une période de match</label>
        <input type="text" id="dateDebut" name="dateDebut"  style="display: inline" placeholder="23.09.2015" required autofocus>
        <input type="text" id="dateFin" name="dateFin"  style="display: inline" placeholder="30.09.2015" required >
        <button class="btn btn-xs btn-primary" type="submit">Rechercher</button>
    </form>

    <table class="table table-condensed table table-bordered" >
        <thead>
            <tr >
                <th >Equipe local</th>
                <th >vs</th>
                <th >Equipe Visiteur</th>
                <th >Date de d�but</th>
                <th >Heure de d�but</th>
            </tr>
        </thead>

        <tbody>
        {% for match in matches %}

                <tr >
                    <td > {{ match.match_localteam_name}}</td>
                    <td ><a href="{{ app.url_generator.generate('pronostic') }}?match_id='{{ match.match_id }}'"> <>  </a></td>
                    <td >{{ match.match_visitorteam_name}}</td>
                    <td >{{ match.match_formatted_date}}</td>
                    <td >{{ match.match_time}}</td>
                </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}

